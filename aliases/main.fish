# Confirm before overwriting files
alias cp="cp -i"
alias mv='mv -i'
alias rm='rm -i'

# Better finging files
alias f="find . | grep "

# Set Autojump to j
alias j="z"

# l as ls
alias l="ls"

# Human readable flags
alias df="df -h"
alias free="free -h"
